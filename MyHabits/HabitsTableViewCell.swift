import UIKit

class HabitsTableViewCell: UITableViewCell {
    
    func configure(with post: Habit) {
        //labelView.text = post.author
        //photoView.image = UIImage(named: post.image)
    }
}
